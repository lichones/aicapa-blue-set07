# [실전세트07·블루] 3과목 — 자동화 제출물

## 배포 URL

- 로컬 검증 주소: `http://127.0.0.1:8000/`
- 실제 제출용 배포 URL: `https://ssophiz.github.io/aicapa-blue-set07/`
- 호스팅 방식: GitHub Pages (`ssophiz/aicapa-blue-set07` 저장소)
- 주의: `cdsa.site`는 사용하지 않으며, 실제 제출 URL은 최소 30일간 유지해야 함.

## 제출 파일

- `index.html`: 분야별 카드, 분야/지역 검색, 결과 건수, 카드뉴스 이미지가 포함된 정적 페이지
- `style.css`, `app.js`, `data.js`: 페이지 스타일·검색 동작·표시 데이터
- `solution.py`: `모집공고.csv`를 분야별로 분리하고 집계표 및 웹 데이터를 생성하는 Python 자동화 도구
- `결과_분야집계.csv`: 분야별 공고 건수와 총 모집인원
- `분야별_csv/모집공고_*.csv`: 분야별 분리 결과

## 실행 방법

1. Python 3에서 `python solution.py`를 실행한다.
2. 현재 폴더에서 `python -m http.server 8000`을 실행한다.
3. 브라우저에서 `http://127.0.0.1:8000/`을 연다.

## 구현 및 검증 체크리스트

- [x] 각 분야 카드에 분야명, 공고 건수, 총 모집인원 표시
- [x] 분야 또는 지역 키워드 검색
- [x] 검색 결과 건수 실시간 표시
- [x] `이미지/카드뉴스1.png`~`카드뉴스5.png` 5장을 `<img>`로 임베드(최소 3장 조건 충족)
- [x] `모집공고.csv` 분야별 CSV 분리
- [x] `결과_분야집계.csv` 생성
- [x] 원본 40건과 분야별 파일의 행 수 합계 일치 확인
- [x] 전체 모집인원과 분야별 총 모집인원 합계 일치 확인
- [ ] GitHub Pages 배포 후 URL 동작 확인 및 30일 유지
